fastapi>=0.109.0
uvicorn[standard]>=0.27.0
httpx>=0.26.0
pydantic>=2.5.0
python-frontmatter>=1.1.0
pyyaml>=6.0
jinja2>=3.1.0
playwright==1.58.0
python-dotenv>=1.0.0
pypdf>=4.0.0
python-multipart>=0.0.9
bleach>=6.1.0
anthropic>=0.69.0
sqlmodel>=0.0.22
# Optional: read API keys from the OS keychain instead of .env (see secret_store.py).
# Degrades gracefully to environment-only resolution if a backend is unavailable.
keyring>=24.0.0

# --- Job Monitor (v7): the JobSpy-backed boards -----------------------------------------------
# LinkedIn / Indeed / Glassdoor / Google Jobs go through `python-jobspy`, which is NOT installed
# by this file. Its published metadata hard-pins `NUMPY==1.26.3` and `regex<2025`, and neither
# has a wheel for CPython 3.14 (numpy 1.26.3 does not compile against it either) -- so listing it
# here would make `pip install -r requirements.txt` fail outright on this project's own venv.
# Those pins are stale rather than real: the library runs fine on the modern versions below.
#
# Install the four boards with:
#     pip install -r requirements.txt
#     pip install --no-deps -r requirements-jobspy.txt
#
# Everything else keeps working without them: `services/jobboards/jobspy_board.py` imports
# `jobspy` lazily, so with the package absent those four boards report Board Status `error` with
# an actionable message and the other three (Remotive, We Work Remotely, Remote OK) are
# unaffected. These are jobspy's actual runtime needs, wheel-available and installed above:
pandas>=2.1,<3.0
beautifulsoup4>=4.12.2
markdownify>=0.13.1,<0.14.0
regex>=2024.4.28
tls-client>=1.0.1,<2.0.0
