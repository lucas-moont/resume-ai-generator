# Dev-only tooling (not needed to run the app). Install with:
#   pip install -r apps/api/requirements-dev.txt
# then, from the repo root:  pre-commit install
pre-commit>=3.5.0
detect-secrets>=1.5.0

# Test suite (httpx is already in requirements.txt; ASGITransport lives there).
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
